import tkinter
import random

ROWS = 30
COLS - 30
TILE_SIZE = 30

WINDOW_WIDTH = TILE_SIZE * COLS   # 30*30 = 900
WINDOW_HEIGHT = TILE_SIZE * ROWS  # 30*30 = 900


class Tile:
    def __init__(self, x, y)
        self.x = x
        self.y = y
